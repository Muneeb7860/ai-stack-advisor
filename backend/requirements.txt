fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.8
pydantic-settings>=2.4
alembic>=1.13
anthropic>=0.34
# >=2.0 specifically: app/mcp/server.py imports MCPServer from mcp.server.mcpserver, the
# 2.0 rename of the older mcp.server.fastmcp.FastMCP API. A pin below 2.0 would resolve to
# the pre-rename module path and break the import — not just a "get the latest" preference.
mcp>=2.0
# app/retrieval.py: ChromaDB vector store for embeddings-based retrieval over
# docs/use-case-knowledge-base/ — see that module's docstring for why embeddings (via a local
# Ollama model) replaced the earlier TF-IDF/cosine approach, and why ChromaDB's own bundled
# default embedding function was rejected (it fetches from Hugging Face at runtime).
chromadb>=1.5
# httpx: used directly by app/retrieval.py to call the local Ollama /api/embed endpoint, and
# by app/llm_providers.py for the local-model chat/tool-calling fallback. Already an
# indirect dependency via fastapi/anthropic, pinned here because both modules import it
# directly rather than relying on it being present transitively.
httpx>=0.27
